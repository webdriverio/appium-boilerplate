// export async function getVisibleElement (selector: string): Promise<boolean> {
//     let array = $$(selector);

//     array.find(element => element.isDisplayed());
    
//     return array[0].isDisplayed();
// }

